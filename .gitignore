/target

# Runtime index artifacts (generated by the indexer, incl. the eval corpus index)
.code-index/
src/.code-index/
.fileSystem-index/
.fileSystem-index-eval/
.fileSystem-index*/

# ONNX model and tokenizer files (must be downloaded separately)
*.onnx
tokenizer.json
tokenizer_config.json
special_tokens_map.json
config.json

# SQLite
*.db
*.db-shm
*.db-wal

# IDE / Editor
.idea/
.vscode/
*.sw?
.DS_Store
Thumbs.db

# Claude Code (local settings)
.claude/settings.local.json
